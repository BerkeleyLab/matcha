name: Build with OpenCoarrays

on: [push, pull_request]

jobs:
  gfortran-build:
    runs-on: [ubuntu-latest]

    env:
      FC: gfortran
      GCC_V: 12

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
